const fs = require('fs'); const files = fs.readdirSync('.').filter(f => f.endsWith('.html')); files.forEach(f => { let c = fs.readFileSync(f, 'utf8'); let modified = false; if (c.match(/Google ile .ye Ol/)) { c = c.replace(/Google ile .ye Ol/g, 'Google ile Üye Ol'); modified = true; } if (c.includes('href="#"')) { c = c.replace(/href="#"/g, 'href="javascript:void(0)"'); modified = true; } if (modified) { fs.writeFileSync(f, c, 'utf8'); console.log('Fixed ' + f); } });
